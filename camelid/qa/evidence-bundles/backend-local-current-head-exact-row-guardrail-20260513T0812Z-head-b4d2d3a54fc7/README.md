# Backend local current-head exact-row guardrail — 20260513T0812Z

Local-only backend/API guardrail slice on current `main` head `b4d2d3a` (`b4d2d3a54fc773d2e2be65ccc2617501ef835e1a`). No SSH, no Ubuntu validation host, no model server promotion rerun, no saturation workload, and no broad support claim was attempted.

## Commands

- `./scripts/with-rustup-cargo.sh test capabilities --all-targets`
- `node scripts/check-public-evidence-claims.mjs`
- `scripts/check-public-scrub.sh`
- `git diff --check`

## Result

PASS: all commands exited 0.

## Boundary

This validates support-surface honesty on the current frontend-polish head only. Mixtral remains bounded/partial and blocked after early-token parity by existing Gate 9A later-generation divergence plus long-continuation HTTP hang artifacts. Llama rows remain exact-row bounded only where row-specific evidence exists. No neighboring-row, arbitrary-template, frontend/API/WebUI/RSS, long-context, production, throughput, portability, or full-support promotion is claimed.
