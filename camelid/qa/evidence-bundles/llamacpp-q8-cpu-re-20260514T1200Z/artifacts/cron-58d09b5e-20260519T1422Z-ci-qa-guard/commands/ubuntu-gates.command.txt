ssh <canonical-ubuntu-host>
# Fresh interactive canonical SSH connection was opened first.
# Remote gate payload used the canonical Ubuntu identity/host and then ran:
set -euo pipefail
export PATH="$HOME/.cargo/bin:$PATH"
WORK="$HOME/work/cron-58d09b5e-20260519T1422Z-ci-qa-guard/repo"
rm -rf "$HOME/work/cron-58d09b5e-20260519T1422Z-ci-qa-guard"
mkdir -p "$(dirname "$WORK")"
git clone --depth 1 --branch buildqa/clippy-needless-range-loop-58d09b5e-20260518T2100Z https://github.com/timtoole02/Camelid.git "$WORK"
cd "$WORK"
date -u +%Y-%m-%dT%H:%M:%SZ
git rev-parse HEAD
git status --short --branch
rustc --version
cargo --version
node --version || true
cargo fmt --all -- --check
cargo check --all-targets --all-features
cargo clippy --all-targets --all-features -- -D warnings
cargo test --all-targets --all-features
cargo doc --no-deps --all-features
