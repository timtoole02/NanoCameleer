- CAMELID TPM LLAMACPP RE MAC Q8: host/build proof is Apple Silicon native arm64 on macOS/Darwin 25.5.0, Apple M4, `FEAT_DotProd=1`, `FEAT_I8MM=1`; Camelid current head `2d95ff5edf1c2c162f9e661ee5adfa6c043e9ef6`, llama.cpp `834a243664114487f99520370a7a7b00fc7a486f`; evidence: `logs/re-run-env-camelid-20260514T200654Z.txt`, `logs/re-run-llamacpp-build-runtime-proof-20260514T201648Z.txt`.
- CAMELID TPM LLAMACPP RE MAC Q8: llama.cpp's actual Mac path is CPU-only for this proof (`GGML_METAL=OFF`, `-ngl 0`, runtime warning says no usable GPU; no Metal path), while CPU reports `NEON=1 | MATMUL_INT8=1 | DOTPROD=1 | SME=1 | ACCELERATE=1 | REPACK=1`; Accelerate/BLAS is linked, but Q8_0 dense speed comes from GGML `CPU_REPACK` + arm int8 kernels, not Metal or generic Accelerate matmul.
- CAMELID TPM LLAMACPP RE MAC Q8: exact Q8_0 dispatch is proven from source: `GGML_TYPE_Q8_0` selects `q8_0_4x8_q8_0` when NEON+MATMUL_INT8 and rows are `%4==0`, otherwise `q8_0_4x4_q8_0` when NEON+DOTPROD and rows are `%4==0`; `CPU_REPACK set_tensor` writes backend-owned packed tensor data (`block_q8_0x4`, 4-row groups, 4/8 interleave) consumed directly by Q8 matmul; evidence: `logs/re-run-llamacpp-q8-path-source-excerpts-20260514T201648Z.txt`.
- CAMELID TPM LLAMACPP RE MAC Q8: Camelid port architecture is now the right class of fix, not the failed duplicate packed-copy: gated `CAMELID_MAC_Q8_REPACK=on`, default off, stores Q8_0 as backend-owned `Q8_0RuntimeStorage::PackedRows4` and Q8 linear ops consume it directly. Landed slices cover Llama 3.2 3B `blk.*.attn_q.weight` plus follow-up `blk.*.ffn_gate.weight`/`blk.*.ffn_up.weight`; legacy `CAMELID_Q8_0_PACKED_4X4_DOT` and `CAMELID_Q8_0_PACKED_4X8_DOT` remain explicit debug/parity sidecars only.
- CAMELID TPM LLAMACPP RE MAC Q8: parity/build gates passed on release: `cargo fmt --check`, focused runtime-storage tests for attn_q and FFN gate/up, `cargo test --release -q q8_0 --lib`, `cargo test --release -q mac_q8_repack_loads --test tensor_store`, and `cargo build --release -q`; evidence: `logs/runtime-packed-storage-tests-attn-q-20260514T203935Z.txt`, `logs/current-head-runtime-packed-tests-20260514T204940Z.txt`, `logs/runtime-packed-storage-tests-attnq-gateup-20260514T205551Z.txt`.
- CAMELID TPM LLAMACPP RE MAC Q8: same-host comparison used Llama 3.2 3B Instruct Q8_0, 8 threads, `-ngl 0`, same prompt/harness. 512-token Camelid runtime-owned attn_q+gate/up preserved baseline token hash and improved generate time from `54.070s` to `49.421s` (`10.360 tok/s`), near llama.cpp CPU `48.711s` (`10.511 tok/s`); evidence: `logs/same-host-3b-runtime_owned_attnq_gateup-512-20260514T205551Z.json`, `logs/same-host-3b-runtime_owned_attnq_gateup-512-20260514T205551Z.server.log.txt`, `logs/final-runtime-owned-attnq-gateup-state-20260514T205551Z.json`.
- CAMELID TPM LLAMACPP RE MAC Q8: latest 1-token matrix compared llama.cpp CPU-only vs Camelid baseline/packed-off/legacy 4x4/legacy 4x8/runtime-owned on the same workload; it is noisy but shows sidecar copies are not final architecture and llama.cpp remains far faster on TTFT. Evidence: `logs/same-host-3b-q8-matrix-summary-20260514T2103Z.txt`. Blocker: `CAMELID_MAC_Q8_REPACK=on CAMELID_Q8_0_BLOCK_DOT=off` can crash on empty file-backed tensor data, so packed-off must mean repack/sidecars off while Q8 block-dot stays on until that fallback guard is fixed. Next owner: Camelid backend/perf should keep runtime-owned Q8 storage, add direct kernels for the remaining dense Llama linear families, and only then promote a speed claim.
